# Alpha14 — RTU-H2C — Long-run 500 kbaud + 100 us + TCP500

Fecha: 2026-09-26

## Objetivo

Confirmar durante 600 s el candidato de producto seleccionado despues de H2B:

```txt
BAUD=500000
CONFIG=8N1
FRAME_GAP=100 us
MOTOR=ASYNC
TX=QUEUED
TCP_TARGET=500 req/s
TCP_FC03_QUANTITY=125
FULL_RUNTIME=ACTIVE
RS485=A/B/GND
W5500=26 MHz
DURATION=600 s
```

No se compila ni sube firmware. Se reutiliza el firmware H2 actualmente
cargado, que ya contiene los comandos de 500 kbaud y 100 us.

## Medicion TCP

Se mantiene una unica conexion TCP durante los 600 s.

Se generan buckets nominales de 60 s para medir:

- req/s por minuto;
- AVG/P95/P99 por minuto;
- minimo/maximo de throughput;
- promedio primera mitad;
- promedio segunda mitad;
- deriva porcentual entre mitades.

Tambien se calcula la latencia global AVG/P95/P99/MAX.

## Medicion RTU

RTU trabaja unpaced durante toda la misma ventana de 600 s.

Al final se exige:

- started = completed = success;
- rejected = 0;
- failed = 0;
- verify fails = 0;
- timeout = 0;
- CRC Master = 0;
- CRC Slave = 0;
- frames RX/TX/OK del Slave coherentes con Master.

## Runtime industrial

Se exige:

- TCP >= 99 % del target global;
- cada bucket TCP >= 99 % del target;
- cero timeout/transporte/protocolo TCP;
- cero frame timeout/bus lock timeout/protocol error del servidor;
- SD failed commits = 0;
- peripheral failure count = 0;
- baud solicitado y efectivo = 500000;
- gap efectivo = 100 us;
- motor ASYNC;
- TX queued.

## Criterio de estabilidad temporal

La deriva entre el promedio de los primeros cinco buckets y los ultimos cinco
se reporta como telemetria. Una deriva grande no se oculta aunque no haya
errores.

El gate reportara:

```txt
RTUH2C_TCP_BUCKET_MIN_REQ_S
RTUH2C_TCP_BUCKET_MAX_REQ_S
RTUH2C_TCP_FIRST_HALF_AVG_REQ_S
RTUH2C_TCP_SECOND_HALF_AVG_REQ_S
RTUH2C_TCP_HALF_DRIFT_PCT
RTUH2C_RTU_HZ
A14_RTU_H2C
```

## Decision posterior

Si H2C queda limpio, 500000 + 100 us pasa de candidato corto a candidato
qualificationado para la matriz JWPLC_FAST/AUTO.

Despues se retoma la investigacion documental del comportamiento anomalo a
230400 baud antes de cerrar la politica final de baudrates.

## Gate versionado

H2C reutiliza el firmware H2 actualmente cargado y no compila ni sube.

Antes de ejecutar exige:

```txt
CORE_A_SHA256=4BFF8C8241DA2E8BD0E1BBA99835ADDF91B9085A05C4DFBD05C339B824794566
MODBUS_RTU_ARCHIVE_SHA256=444BE3A04079A579252B2737FE6070E00ADCA949FD176880588FE69561B2A79F
TRACKED_DIRTY=solo core candidato
STAGED_COUNT=0
```

El runner vuelve a configurar por USB de diagnostico:

```txt
500000 baud
100 us
ASYNC
QUEUED
RTU UNPACED
```

y valida el perfil por snapshot antes de iniciar la ventana.

La conexion TCP se mantiene continua durante 600 s. Se imprimen buckets de 60 s
en vivo para detectar degradacion temporal. Cualquier error TCP/RTU o fallo de
runtime hace fallar el gate.

Al cierre se pide confirmacion fisica de ambas TFT y se vuelven a comprobar los
hashes, el dirty scope y el indice Git.

## Resultado fisico RTU-H2C

Perfil confirmado:

```txt
500000 baud
100 us
ASYNC
TX QUEUED
TCP500 FC03/125
RTU UNPACED
600 s
```

| Metrica | Resultado |
|---|---:|
| TCP global | 500.000 req/s |
| TCP target | 100.000 % |
| Bucket minimo | 499.950 req/s |
| Bucket maximo | 500.050 req/s |
| Primera mitad | 500.000 req/s |
| Segunda mitad | 500.000 req/s |
| Deriva entre mitades | 0.000 % |
| TCP AVG | 1300.7 us |
| TCP P95 | 2132.8 us |
| TCP P99 | 9659.1 us |
| TCP MAX | 203095.7 us |
| RTU | 420.916 Hz |
| RTU completed/success | 252625 / 252625 |
| RTU failed/timeout | 0 / 0 |
| CRC Master/Slave | 0 / 0 |
| SD accepted | 19168 B |
| SD committed | 19072 B |
| SD pending | 96 B |
| SD failed commits | 0 |
| Peripheral failures | 0 |

```txt
RTUH2C_PROFILE_PASS=YES
RTUH2C_TCP_CLEAN=YES
RTUH2C_BUCKET_TARGET_PASS=YES
RTUH2C_RTU_CLEAN=YES
RTUH2C_SD_CLEAN=YES
RTUH2C_RUNTIME_CLEAN=YES
A14_RTU_H2C=PASS_LONGRUN_600S
```

500000 + 100 us queda qualificationado en este banco como candidato
JWPLC_FAST/AUTO. El maximo TCP de 203095.7 us se conserva como outlier
documentado; no produjo timeout, perdida de target ni deriva temporal.

